spring:
  datasource:
    url: jdbc:mysql://localhost:3306/scenery?useSSL=false&useUnicode=true&characterEncoding=utf8mb4&serverTimezone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate.format_sql: true
logging:
  level:
    root: info
    org.hibernate.SQL: debug
jwt:
  secret: cwxcwx123123
  expire: 86400000
